#job-show-map
.address= @job.postcode
.job-id= @job.id

.job-show-header
  %h3= @job.title
  %h4
    POSTED BY
    = link_to @job.poster.username, profile_path(@job.poster)
  
  - if user_signed_in?
    .bid-status
      - if @job.poster == current_user
        = link_to 'EDIT', edit_job_path
      - elsif @job.helper == current_user
        = YOU HAVE WON THIS JOB
      - elsif @job.applicants.include? current_user
        = YOU HAVE APPLIED
  - else
    .bid-status= link_to 'I CAN HELP', new_job_bid_path(@job)

.job-show-content
  %h4 Description
  %p= @job.description

  - if user_signed_in?
    - if @job.poster == current_user || @job.helper == current_user
      %h4 Instructions
      %p= @job.instructions
      %h4 Agreed time
      %p= @job.date_time.to_formatted_s(:long_ordinal)
      %h4 Address
      %p= @job.address

- if user_signed_in?
  - if @job.poster == current_user
    - if @job.helper
      .job-show-helper
        .embedded-profile
          .embedded-profile-image
            = image_tag(@job.helper.image)
          .embedded-profile-stats
            %h3= @job.helper.username
            %h4= @job.helper.username
            
    - else
      .job-show-select-helper
        There are no offers to display yet.

-# - if @job.poster == current_user
-#   .page-nav
-#     = link_to 'EDIT', edit_job_path
-#     = link_to 'BACK', jobs_path

-# - if @job.poster == current_user
-#   %h3 APPLICATIONS
-#   %table
-#     %tr
-#       %td User
-#       %td Note
-#       %td Select
-#     - @job.bids.each do |bid|
-#       %tr#choose_helper_table
-#         %td= link_to bid.applicant.username, profile_path(bid.applicant)
-#         %td= bid.note
-#         %td
-#           %button.choose_helper_button{'data-id' => bid.job_id, 'data-helper-id' => bid.applicant.id} Choose
/           / button is pressed by elder, their id is already entered into the job table as current user thru devise. this button needs to enter the applicant_id (from the bid table), into the helper_id in the job table. Then redirect to dashboard
  
-# %button#choose-helper-button