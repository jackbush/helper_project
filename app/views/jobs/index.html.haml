
%button#toggle toggle map

#job-index-map

/ %h4 search for a job
/ %input{type: "search", id: "search-keyword"}
/ %input{type: "submit", value: 'submit'}


- @jobs.each do |job|
  .well.current-user 
    .job-post-index
      %h3= link_to job.title, job_path(job)
      %h4= job.poster.username
      %h4= job.description
      %h4= job.postcode
    - if job.applicants.include? current_user
      %span.bid-status YOU HAVE APPLIED
